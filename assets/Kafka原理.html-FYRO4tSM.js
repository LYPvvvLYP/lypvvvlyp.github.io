import{_ as e}from"./plugin-vue_export-helper-DlAUqK2U.js";import{o as a,c as r,a as o}from"./app-BpO3Vn9P.js";const n="/assets/Pasted%20image%2020221124175254-B6VEKKZc.png",s="/assets/Pasted%20image%2020221124175340-DFZ1aVpF.png",i="/assets/Pasted%20image%2020221124175819-Bc0N2aFn.png",t="/assets/Pasted%20image%2020221124180247-Dpkt0UFm.png",p="/assets/Pasted%20image%2020221124180507-BUhomNmD.png",c="/assets/Pasted%20image%2020221124182527-DLVDT6jw.png",d={},l=o('<h1 id="kafka-原理" tabindex="-1"><a class="header-anchor" href="#kafka-原理"><span>Kafka 原理</span></a></h1><h2 id="概念" tabindex="-1"><a class="header-anchor" href="#概念"><span>概念</span></a></h2><p><img src="'+n+'" alt=""></p><p>在 Kafka 架构中，有几个术语需要了解下。</p><ul><li><strong>Producer：</strong>  生产者，即消息发送者，Push 消息到 Kafka 集群的 Broker（就是 Server）中；</li><li><strong>Broker：</strong> Kafka 集群由多个 Kafka 实例（Server）组成，每个实例构成一个 Broker，其实就是服务器；</li><li><strong>Topic：</strong> Producer 向 Kafka 集群 Push 的消息会被归于某一类别，即 Topic。本质上，这只是一个逻辑概念，面向的对象是 Producer 和 Consumer，Producer 只需关注将消息 Push 到哪一个 Topic 中，而 Consumer 只需关心自己订阅了哪个 Topic；</li><li><strong>Partition：</strong>  每个 Topic 又被分为多个 Partition，即物理分区。出于负载均衡的考虑，同一个 Topic 的 Partition 分别存储于 Kafka 集群的多个 Broker 上。而为了提高可靠性，这些 Partition 可以由 Kafka 机制中的 Replicas 来设置备份的数量。如上面框架图所示，每个 Partition 都存在两个备份；</li><li><strong>Consumer：</strong>  消费者，从 Kafka 集群的 Broker 中 Pull 消息、消费消息；</li><li><strong>Consumer Group：</strong> High-Level Consumer API 中，每个 Consumer 都属于一个 Consumer Group，每条消息只能被 Consumer Group 中的一个 Consumer 消费，但可以被多个 Consumer Group 消费；</li><li><strong>Replicas：</strong> Partition 的副本，保障 Partition 的高可用；</li><li><strong>Leader：</strong> Replicas 中的一个角色， Producer 和 Consumer 只与 Leader 交互；</li><li><strong>Follower：</strong> Replicas 中的一个角色，从 Leader 中复制数据，作为它的副本，同时一旦某 Leader 挂掉，便会从它的所有 Follower 中选举出一个新的 Leader 继续提供服务；</li><li><strong>Controller：</strong> Kafka 集群中的一个服务器，用来进行 Leader Election 以及各种 Fail Over；</li><li><strong>Zookeeper：</strong> Kafka 通过 ZooKeeper 存储集群的 Meta 信息等，文中将详述。</li></ul><h3 id="topic-partition" tabindex="-1"><a class="header-anchor" href="#topic-partition"><span>Topic &amp; Partition</span></a></h3><p>为了便于区分消息，Producer 向 Kafka 集群 Push 的消息会被归于某一类别，即 Topic。为了负载均衡、增强可扩展性，Topic 又被分为多个 Partition。从存储层面来看，每一个 Partition 都是一个有序的、不可变的记录序列，通俗点就是一个追加日志（Append Log）文件。每个 Partition 中的记录都会被分配一个称为偏移量（Offset）的序列 ID 号，该序列 ID 号唯一地标识 Partition 内的每个记录。</p><p>Kafka 机制中，Producer Push 的消息是追加（Append）到 Partition 中的，这是一种顺序写磁盘的机制，效率远高于随机写内存，如下图所示：</p><p><img src="'+s+`" alt=""></p><h4 id="kafka-为什么要将-topic-分区" tabindex="-1"><a class="header-anchor" href="#kafka-为什么要将-topic-分区"><span>Kafka 为什么要将 Topic 分区？</span></a></h4><blockquote><p>简而言之：负载均衡 + 水平扩展。</p></blockquote><p>前已述及，Topic 只是逻辑概念，面向的是 Producer 和 Consumer，而 Partition 则是物理概念。可以想象，如果 Topic 不进行分区，而将 Topic 内的消息存储于一个 Broker，那么该 Topic 的所有读写请求都将由这个 Broker 处理，吞吐量很容易陷入瓶颈，这显然不适合高吞吐量应用场景。</p><p>有了 Partition 概念，假设一个 Topic 被分为 10 个 Partition，Kafka 会根据一定的算法将 10 个 Partition 尽可能均匀地分布到不同的 Broker（服务器）上。当 Producer 发布消息时，Producer 客户端可以采用 Random、Key-Hash 及轮询等算法选定目标 Partition。若不指定，Kafka 也将根据一定算法将其置于某一分区上。Partiton 机制可以极大地提高吞吐量，并且使系统具备良好的水平扩展能力。</p><h2 id="kafka-高可靠性实现基础" tabindex="-1"><a class="header-anchor" href="#kafka-高可靠性实现基础"><span>Kafka 高可靠性实现基础</span></a></h2><h3 id="kafka-文件存储机制" tabindex="-1"><a class="header-anchor" href="#kafka-文件存储机制"><span>Kafka 文件存储机制</span></a></h3><p>上文已经介绍了 Topic 和 Partition。事实上，Partition 并不是最终的存储粒度，Partition 还可以进一步细分为 Segment。换言之，一个 Partition 物理上由多个 Segment 组成，Segment 才是实际的存储粒度。</p><div class="language-yaml line-numbers-mode" data-ext="yml" data-title="yml"><pre class="language-yaml"><code>drwxr<span class="token punctuation">-</span>xr<span class="token punctuation">-</span>x 2 root root 4096 Oct 15 13<span class="token punctuation">:</span>21 mytopic_test<span class="token punctuation">-</span><span class="token number">0</span>
drwxr<span class="token punctuation">-</span>xr<span class="token punctuation">-</span>x 2 root root 4096 Oct 15 13<span class="token punctuation">:</span>21 mytopic_test<span class="token punctuation">-</span><span class="token number">1</span>
drwxr<span class="token punctuation">-</span>xr<span class="token punctuation">-</span>x 2 root root 4096 Oct 15 13<span class="token punctuation">:</span>21 mytopic_test<span class="token punctuation">-</span><span class="token number">2</span>
drwxr<span class="token punctuation">-</span>xr<span class="token punctuation">-</span>x 2 root root 4096 Oct 15 13<span class="token punctuation">:</span>21 mytopic_test<span class="token punctuation">-</span><span class="token number">3</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>创建了一个名为“mytopic_test”的 Topic，同时为它指定了 4 个 Partition，也就是上面的 4 个文件夹（由于 Partition 不是最终的存储粒度，所以是文件夹而不是文件）。Partition 的名称规则为：Topic 名称+索引号，索引的范围为：<code>[0，num.partitions - 1]</code>。</p><h4 id="为什么不能以-partition-作为存储单位" tabindex="-1"><a class="header-anchor" href="#为什么不能以-partition-作为存储单位"><span>为什么不能以 Partition 作为存储单位？</span></a></h4><p>任何一个 Topic 中的 Partition 数量受限于 Kafka Broker 的数量，不可能太多。如果以 Partition 为存储粒度，随着消息源源不断写入，数量有限的 Partition 将急剧扩张，会对消息文件的维护以及对已消费消息的清理工作带来严重影响。</p><p>Partition 的数量为什么受限于 Kafka Broker 的数量？为了保证可靠性，每个 Partition 都应有若干个副本（Replica），其中一个 Replica 为 Leader，其它都为 Follower。Leader 负责处理 Partition 的所有读写请求，Follower 则负责被动地复制 Leader 上的数据。不难理解，对于任意一个 Topic，有多少个 Partition 就有多少个 Leader，并且，这些 Leader 应尽量分散到不同的 Broker 上，否则，一旦某台 Broker（如果它部署有大量 Leader）故障下线，势必引起连锁反应：大量的 Partition 需要重选 Leader，而这期间是不可用的。</p><p>鉴于上述原因，有必要对 Partition 进一步细分，细分的单位就是 Segment。很容易理解，一个 Partition（文件夹）相当于一个巨大的文件，通过细分，被切分为多个大小相等的 Segment 文件，但由于消息大小不同，每个 Segment 中的消息数量通常是不同的。基于 Segment 机制，可以很方便地清理那些陈旧的消息（已被消费的消息），从而提高磁盘的利用率。</p><h4 id="segment-的工作原理" tabindex="-1"><a class="header-anchor" href="#segment-的工作原理"><span>Segment 的工作原理</span></a></h4><p>打开一个 Partition 文件夹（目录），如下所示：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>00000000000000000000.index
00000000000000000000.log
00000000000000170410.index
00000000000000170410.log
00000000000000239430.index
00000000000000239430.log
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>并没有发现“Segment”文件！事实上，Segment 文件由两部分组成，即  <code>.index</code>  文件和  <code>.log</code>  文件，分别为 Segment 索引文件和数据文件。观察上面的文件名，很容易理解它们的命规则，对于索引文件，Partition 全局的第一个 Segment 从 0 开始，后续每个 Segment 文件名为上一个 Segment 文件最后一条消息的偏移量（Offset）值，Offset 的数值由 20 位数字字符表示，没有数字的位置用 0 填充。对于数据文件，其命名与对应的索引文件保持一致即可。</p><p>以上面的其中一“对” Segment 文件为例：<code>00000000000000170410.index</code>  和  <code>00000000000000170410.log</code>，绘制其关系图，如下所示：</p><p><img src="`+i+'" alt=""></p><p><code>.index</code>  文件作为索引文件，存储的是元数据；<code>.log</code>  文件作为数据文件，存储的是消息。如何通过索引访问具体的消息呢？事实上，索引文件中的元数据指向的是对应数据文件中消息的物理偏移地址，有了消息的物理地址，自然也就可以访问对应的消息了。</p><p>其中以  <code>.index</code>  索引文件中的元数据  <code>[2, 365]</code>  为例，在  <code>.log</code>  数据文件表示第 2 个消息，即在全局 Partition 中表示 170410+2=170412 个消息，该消息的物理偏移地址为 365。</p><h4 id="partition-中通过-offset-查找-message" tabindex="-1"><a class="header-anchor" href="#partition-中通过-offset-查找-message"><span>Partition 中通过 Offset 查找 Message</span></a></h4><p>根据 Offset 定位到一个 Segment 上，然后通过 Segment 里的元数据查到 Segment 里的物理偏移。</p><h3 id="复制原理和同步方式" tabindex="-1"><a class="header-anchor" href="#复制原理和同步方式"><span>复制原理和同步方式</span></a></h3><p>提高可用性，冗余设计（也称副本机制）是常用的策略，Kafka 也不例外。在 Kafka 中，副本是以 Partition 为单位，换言之，每个 Partition 有 N 个副本（Replica）</p><p>基于多副本机制，Kafka 可实现故障自动转移（Fail Over）。副本根据角色的不同可分为以下三类。</p><ul><li>Leader 副本：响应 Client 端读写请求的副本，Producer 和 Consumer 只跟 Leader 交互；</li><li>Follower 副本：被动地备份 Leader 副本中的数据，不能响应 Client 端读写请求。一旦 Leader 挂掉，会从它的 Follower 中选举出一个新的 Leader 继续提供服务；</li><li>ISR 副本：包含了 Leader 副本和所有与 Leader 副本保持同步的 Follower 副本。如何判定是否与 Leader 同步，后面详述。</li></ul><p>一个具有 4 个 Broker 的 Kafka 集群，TopicA 有 3 个 Partition，每个 Partition 有 3 个副本（Leader+Follower）。</p><p><img src="'+t+'" alt=""></p><h4 id="如何确保新选举出的-leader-是最优秀的" tabindex="-1"><a class="header-anchor" href="#如何确保新选举出的-leader-是最优秀的"><span>如何确保新选举出的 Leader 是最优秀的？</span></a></h4><p>一个 Partition 有多个副本（Replica），为了提高可靠性，这些副本分散在不同的 Broker 上。由于带宽、读写性能、网络延迟等因素，同一时刻，这些副本的状态通常是不一致的，即 Follower 与 Leader 的状态不一致。那么，如何保证新 Leader 是优选出来的呢？</p><p>Kafka 机制中，Leader 将负责维护和跟踪一个 ISR（In-Sync Replicas）列表，即同步副本队列，这个列表里面的副本与 Leader 保持同步，状态一致。如果新的 Leader 从 ISR 列表里的副本中选出，那么就可以保证新 Leader 是优秀的。当然，这不是唯一的策略，下文将继续解读。</p><h4 id="同步副本-isr" tabindex="-1"><a class="header-anchor" href="#同步副本-isr"><span>同步副本 ISR</span></a></h4><p>上节中讲到了同步副本队列 ISR（In-Sync Replicas），即 Leader 副本 + 所有与 Leader 副本保持同步的 Follower 副本。虽然副本可以提高可用性，但副本数量对 Kafka 的吞吐率有一定影响。默认情况下 Kafka 的 Replica 数量为 2（部分 Kafka 版本默认值为 1），即每个 Partition 都有一个 Leader，一个 Follower。所有的副本（Replica）统称为 Assigned Replicas（AR）。显然，ISR 是 AR 中的一个子集，由 Leader 维护 ISR 列表，Follower 从 Leader 那里同步数据会有一些延迟（由参数  <code>replica.lag.time.max.ms</code>  设置超时阈值），超过阈值的 Follower 将被剔除出 ISR， 存入 OSR（Outof-Sync Replicas）列表，新加入的 Follower 也会先存放在 OSR 中，即有关系式 AR=ISR+OSR。</p><p>只要保证 ISR 中的 Replica 数量大于 2（ISR 包括 Leader），即便出现 Leader 突然故障下线的情况，也能保证消息不丢失（因为 ISR 中的 Replica 与 Leader 保持同步）。当然，凡事过犹不及，ISR 中 Replica 的数量不宜过多，否则会降低 Kafka 的吞吐性能。</p><p>补充一点，OSR 内的 Replica 是否同步了 Leader 的数据不影响数据是否提交成功，这些 Replica 会不断从 Leader 中同步数据。至于同步的进度并不重要，不过，运维人员应密切关注 Replica 从 ISR 中降级转入 OSR 的情况，并及时排查故障，使其尽快回到 ISR 中，以维持 ISR 中 Replica 的数量处于合理状态，同时降低集群宕机的风险。</p><h4 id="leo-hw" tabindex="-1"><a class="header-anchor" href="#leo-hw"><span>LEO &amp; HW</span></a></h4><p><img src="'+p+'" alt=""></p><ul><li>LEO：即日志末端位移（Log End Offset），表示每个副本的 Log 最后一条 Message 的位置。比如 LEO=10、HW=7，则表示该副本保存了 10 条消息，而后面 3 条处于 Uncommitted 状态。</li><li>HW：即水位值（High Watermark）。对于同一个副本而言，其 HW 值不大于 LEO 值。小于等于 HW 值的所有消息都被认为是“已备份”的（Replicated），对于任何一个 Partition，取其对应的 ISR 中最小的 LEO 作为 HW，Consumer 最多只能消费到 HW 所在的位置。</li></ul><p>此外，需要说明的是，每个 Replica （包含 Leader 和 Follower）都有 HW，它们各自负责更新自己的 HW 的状态。</p><p>Kafka 的复制机制既不是完全的同步复制，也不是单纯的异步复制。同步复制要求所有能工作的 Follower 都复制完，这条消息才会被置为 Committed 状态，该复制方式受限于复制最慢的 Follower，会极大地影响吞吐率，因而极少应用于生产环境。而异步复制方式下，Follower 异步地从 Leader 复制数据，数据只要被 Leader 写入 Log 就被认为已经 Committed（类似 Redis，主从异步复制）。如果在 Follower 尚未复制完成的情况下，Leader 宕机，则必然导致数据丢失，很多时候，这是不可接受的。</p><p>相较于完全同步复制和异步复制，Kafka 使用 ISR 的策略则是一种较“中庸”的策略，在可靠性和吞吐率方面取得了较好的平衡。某种意义上，ISR 策略与第 8 课中介绍的 Raft 算法所采用的“多数派原则”类似，不过 ISR 更为灵活。</p><h5 id="截断机制" tabindex="-1"><a class="header-anchor" href="#截断机制"><span>截断机制</span></a></h5><p>如果出现 Leader 故障下线的情况，就需要从所有的 Follower 中选举新的 Leader，以便继续提供服务。为了保证一致性，通常只能从 ISR 列表中选取新的 Leader （上面已经介绍，ISR 列表中的 Follower 与原 Leader 保持同步），因此，无论 ISR 中哪个 Follower 被选为新的 Leader，它都知道 HW 之前的数据，可以保证在切换了 Leader 后，Consumer 可以继续“看到”之前已经由 Producer 提交的数据。</p><p>如下图所示，如果 Leader 宕机，Follower1 被选为新的 Leader，而新 Leader （原 Follower1 ）并没有完全同步之前 Leader 的所有数据（少了一个消息 6），之后，新 Leader 又继续接受了新的数据，此时，原本宕机的 Leader 经修复后重新上线，它将发现新 Leader 中的数据和自己持有的数据不一致，怎么办呢？</p><p>为了保证一致性，必须有一方妥协，显然旧的 Leader 优先级较低，因此， 它会将自己的数据截断到宕机之前的 HW 位置（HW 之前的数据，与 Leader 一定是相同的），然后同步新 Leader 的数据。这便是所谓的 “截断机制”。</p><p><img src="'+c+`" alt=""></p><h3 id="kafka-消息生产的可靠性" tabindex="-1"><a class="header-anchor" href="#kafka-消息生产的可靠性"><span>Kafka 消息生产的可靠性</span></a></h3><p>就 Kafka 而言，可靠性贯穿消息的生产、发送、存储及消费等过程，本节将介绍消息生产过程中的可靠性，即当 Producer 向 Leader 发送消息时的可靠性，在客户端，可以通过  <code>request.required.acks</code>  参数来设置数据可靠性的级别。</p><p><strong>（1）request.required.acks = 1</strong></p><p>这是默认情况，即消息的强制备份数量为 1，Producer 发送数据到 Leader，只要 Leader 成功写入本地日志，即成功返回客户端，不要求 ISR 中的其它副本与 Leader 保持同步。对于 Producer 发来的消息，如果 Leader 刚写入并成功返回后便宕机，此次发送的消息就会丢失。</p><p><strong>（2）request.required.acks = 0</strong></p><p>即消息的强制备份数量为 0，Producer 不停向 Leader 发送数据，而不需要 Leader 反馈成功消息，这种情况下数据传输效率最高，与此同时，可靠性也是最低的。可能在发送过程中丢失数据，可能在 Leader 宕机时丢失数据。</p><p><strong>（3）request.required.acks = -1（all）</strong></p><p>即消息的强制备份数量为 ISR 列表中副本的数量，Producer 发送数据给 Leader，Leader 收到数据后要等到 ISR 列表中的所有副本都完成数据同步后（强一致性），才向生产者返回成功消息。如果一直收不到成功消息，则认为发送数据失败会自动重发数据。这是可靠性最高的方案，当然，性能也会受到一定影响。</p><h4 id="单纯设置-request-required-acks-保障不了消息生产的可靠性" tabindex="-1"><a class="header-anchor" href="#单纯设置-request-required-acks-保障不了消息生产的可靠性"><span>单纯设置 request.required.acks 保障不了消息生产的可靠性</span></a></h4><p>回顾之前讲得，由于 Follower 从 Leader 同步数据有一些延迟（由参数  <code>replica.lag.time.max.ms</code>  设置超时阈值），超过阈值的 Follower 将被剔除出 ISR， 存入 OSR（Outof-Sync Replicas）列表，因此，ISR 列表实际上是动态变化的。</p><p>我们思考一个问题。由于 ISR 列表是动态变化的，如果 ISR 中的副本因网络延迟等原因被踢出，只剩下 Leader，即便设置参数  <code>request.required.acks=-1</code>  也无法保证可靠性。鉴于此，需要对 ISR 列表的最小副本数加以约束，即 ISR 列表中的副本数不得小于一个阈值。Kafka 提供了这样一个参数：<code>min.insync.replicas</code>，该参数用于设定 ISR 中的最小副本数，默认值为 1，当且仅当  <code>request.required.acks</code>  参数设置为 -1 时，此参数才生效。当 ISR 中的副本数少于  <code>min.insync.replicas</code>配置的数量时，客户端会返回如下异常：</p><div class="language-sql line-numbers-mode" data-ext="sql" data-title="sql"><pre class="language-sql"><code>org<span class="token punctuation">.</span>apache<span class="token punctuation">.</span>kafka<span class="token punctuation">.</span>common<span class="token punctuation">.</span><span class="token keyword">errors</span><span class="token punctuation">.</span>NotEnoughReplicasExceptoin: Messages are rejected since there are fewer <span class="token operator">in</span><span class="token operator">-</span>sync replicas than required。
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>不难理解，如果  <code>min.insync.replicas</code>  设置为 2，当 ISR 中实际副本数为 1 时（只有 Leader），将无法保证可靠性，此时拒绝客户端的写请求以防止消息丢失。</p><h3 id="leader-选举机制" tabindex="-1"><a class="header-anchor" href="#leader-选举机制"><span>Leader 选举机制</span></a></h3><h4 id="少数服从多数的选举原则" tabindex="-1"><a class="header-anchor" href="#少数服从多数的选举原则"><span>少数服从多数的选举原则</span></a></h4><p>“少数服从多数”是最常用的选举原则，它也称为“多数派原则”，第 8 课中介绍的 Raft 算法就采用了这种选举原则。不过，Kafka 并不是采用这种方式。</p><p>基于该原则，如果有 2f+1 个副本，那么，在一条消息被 Commit 前必须保证有 f+1 个 Replica 复制完消息；同时，失败的副本数不能超过 f 个，否则将丧失选举基础。这种方式有个很出色的特点：延迟只取决于最快的服务器。也就是说，如果复制因子是 3，则延迟是由更快的 Follower 而不是较慢的 Follower 决定的。</p><p>当然，“少数服从多数”的选举策略也有一些不足，为了保证 Leader 选举正常进行，它所能容忍的失败 Follower 数比较少：如果要容忍 m 个 Follower 故障，至少要  <code>2*m+1</code>  个副本。换言之，在生产环境中为了保证较高的容错率，必须维持大量的副本，而过多的副本又会反过来制约性能，鉴于此，这种算法很少在需要保证高吞吐量的系统中使用。</p><p>Kafka 通过 ZooKeeper 为每一个 Partition 动态维护了一个 ISR 列表，通过前面的学习，我们知道 ISR 里的所有 Replica 都与 Leader 保持同步，严格得讲，为了保证可靠性，只有 ISR 里的成员才能有被选为 Leader 的可能（通过参数配置：<code>unclean.leader.election.enable=false</code>）。基于该策略，如果有 f+1 个副本，一个 Kafka Topic 能在保证不丢失已经 Commit 消息的前提下容忍 f 个副本的失败，在大多数使用场景下，这种折衷策略是合理的。</p><p>实际上，对于任意一条消息，只有它被 ISR 中的所有 Follower 都从 Leader 复制过去才会被认为 Committed，并返回信息给 Producer，从而保证可靠性。但与“少数服从多数”策略不同的是，Kafka ISR 列表中副本的数量不需要超过副本总数的一半，即不需要满足“多数派”原则，通常，ISR 列表副本数大于等于 2 即可，如此，便在可靠性和吞吐量方面取得平衡。</p><h3 id="kafka-架构中-zookeeper-以怎样的形式存在" tabindex="-1"><a class="header-anchor" href="#kafka-架构中-zookeeper-以怎样的形式存在"><span>Kafka 架构中 ZooKeeper 以怎样的形式存在？</span></a></h3><p>在基于 Kafka 的分布式消息队列中，ZooKeeper 的作用有 Broker 注册、Topic 注册、Producer 和 Consumer 负载均衡、维护 Partition 与 Consumer 的关系、记录消息消费的进度以及 Consumer 注册等。</p><h4 id="broker-在-zookeeper-中的注册" tabindex="-1"><a class="header-anchor" href="#broker-在-zookeeper-中的注册"><span>Broker 在 ZooKeeper 中的注册</span></a></h4><p>言归正传，Broker 注册，也就是 Kafka 节点注册，本质上就是在 ZooKeeper 中创建一个专属的目录（又称为节点），其路径为  <code>/ brokers</code>。</p><p>读者应该记得，上一课在讲述 Kafka 配置时，特意强调了节点的唯一标识：<code>broker.id</code>。它有什么用呢？在 Broker 启动的同时，需要到配置中心（ZooKeeper）注册，而  <code>broker.id</code>  作为唯一标识，根据它便可在 ZooKeeper 中创建专属节点（目录），其路径为  <code>/brokers/ids/{broker.id}</code>。</p><p>在专属节点创建好后，Kafka 会将该 Broker 相关的信息存入其中，包括  <code>broker.name</code> 、端口号。</p><p>需要特别说明的是，Broker 在 ZooKeeper 中注册的节点是“临时节点”，一旦 Broker 故障下线，ZooKeeper 就会将该节点删除。同时，可以基于 Watch 机制监听到这一删除事件，进而做出响应（如负载均衡）。</p><h4 id="topic-在-zookeeper-中的注册" tabindex="-1"><a class="header-anchor" href="#topic-在-zookeeper-中的注册"><span>Topic 在 ZooKeeper 中的注册</span></a></h4><p>在 Kafka 中，所有 Topic 与 Broker 的对应关系都由 ZooKeeper 来维护，在 ZooKeeper 中，通过建立专属的节点来存储这些信息，其路径为  <code>/brokers/topics/{topic_name}</code>。</p><h4 id="consumer-在-zookeeper-中的注册" tabindex="-1"><a class="header-anchor" href="#consumer-在-zookeeper-中的注册"><span>Consumer 在 ZooKeeper 中的注册</span></a></h4><p>与 Broker、Topic 注册类似，Consumer Group 注册本质上也是在 ZooKeeper 中创建专属的节点，以记录相关信息，其路径为  <code>/consumers/{group_id}</code>。</p><p>这里补充一点，在 ZooKeeper 中，<code>/consumers/{group_id}</code>  虽然被称为节点，但本质上是一个目录。既然是目录，在记录信息时，就可以根据信息的不同，进一步创建子目录（子节点），分别记录不同类别的信息。对于 Consumer Group 而言，有三类信息需要记录，因此，<code>/consumers/{group_id}</code>  下还有三个子目录，如下所示。</p><ul><li>ids：Consumer Group 中有多个 Consumer，ids 用于记录这些 Consumer；</li><li>owners：记录该 Consumer Group 可消费的 Topic 信息；</li><li>offsets：记录 owners 中每个 Topic 的所有 Partition 的 Offset。</li></ul><p><strong>Consumer 注册</strong></p><p>原理同 Consumer Group 注册，不过需要注意的是，其节点路径比较特殊，需在路径  <code>/consumers/{group_id}/ids</code>  下创建专属子节点，它是临时的。比如，某 Consumer 的临时节点路径为  <code>/consumers/{group_id}/ids/my_consumer_for_test-1223234-fdfv1233df23</code>。</p><h3 id="负载均衡" tabindex="-1"><a class="header-anchor" href="#负载均衡"><span>负载均衡</span></a></h3><p>通过前面的学习，我们知道，对于一条消息，订阅了它的 Consumer Group 中只能有一个 Consumer 消费它。那么就存在一个问题：一个 Consumer Group 中有多个 Consumer，如何让它们尽可能均匀地消费订阅的消息呢（也就是负载均衡）？这里不讨论实现细节，但要实现负载均衡，实时获取 Consumer 的数量显然是必要的，通过 Watch 机制监听  <code>/consumers/{group_id}/ids</code>  下子节点的事件便可实现。</p><h4 id="producers-负载均衡" tabindex="-1"><a class="header-anchor" href="#producers-负载均衡"><span>Producers 负载均衡</span></a></h4><p>前面已经介绍过，为了负载均衡和避免连锁反应，Kafka 中，同一个 Topic 的 Partition 会尽量分散到不同的 Broker 上。而 Producers 则根据指定的 Topic 将消息 Push 到相应的 Partition，那么，如何将消息均衡地 Push 到各个 Partition 呢？这便是 Producers 负载均衡的问题。</p><p>Producers 启动后同样也要进行注册（依然是创建一个专属的临时节点），为了负载均衡，Producers 会通过 Watcher 机制监听 Brokers 注册节点的变化。一旦 Brokers 发生变化，如增加、减少，Producers 可以收到通知并更新自己记录的 Broker 列表 。此外，基于 ZooKeeper 提供的 Watcher 机制，还可以监听其它在 ZooKeeper 上注册的节点，如 Topic、Consumer 等。</p><p>Producer 向 Kafka 集群 Push 消息的时候，必须指定 Topic，不过，Partition 却是非必要的。事实上，目前高级的客户端已经不提供指定 Partition 的接口。虽然不提供，但并不代表无须指定 Partition，只是隐藏了细节。通常有两种方式用于指定 Partition。</p><h4 id="consumer-负载均衡" tabindex="-1"><a class="header-anchor" href="#consumer-负载均衡"><span>Consumer 负载均衡</span></a></h4><p>前已述及，Consumer Group、Consumer、Broker 都会在 ZooKeeper 中注册节点，因此，基于 ZooKeeper 提供的 Watcher，Consumer 可以监听同一 Group 中 Consumers 的变化，以及 Broker 列表的变化。进一步，根据 Consumer 列表，将 Partition 排序后，轮流进行分配。由于这是一个动态过程，相应的负载均衡被称为 Rebalance，其描述如下：</p><ol><li>对任意一个 Topic 中所有的 Partirtion 进行排序，用数组 PT 记录；</li><li>某一 Consumer Group 订阅了上述 Topic，对它的所有 Consumer 排序，用数组 CG 记录，第 i 个 Consumer 记为  <code>CG[i]</code>；</li><li>比例系数为  <code>F=size(PT)/size(CG)</code>，向上取整；</li><li>解除  <code>CG[i]</code>  对原来分配的 Partition 的消费权（i 从 0 开始，最大值为  <code>size(CG)-1</code>）;</li><li>将第  <code>i*F</code>  到  <code>(i+1)*F-1</code>  个 Partition 分配给 <code>CG[i]</code>。</li></ol><h4 id="记录消费进度-offset" tabindex="-1"><a class="header-anchor" href="#记录消费进度-offset"><span>记录消费进度 Offset</span></a></h4><p>在 3.3 节中曾提及 Offset，它是  <code>/consumers/[group_id]</code>  下的一个子节点。Kafka 中，Consumer 采用 Pull 模式消费相应 Partition 中的消息，是一种异步消费模式。为了避免因 Consumer 故障、重启、Rebalance 等原因造成重复消费、遗漏消费消息，需要记录 Consumer 对 Partition 中消息的消费进度，即偏移量 Offset。</p><h4 id="记录-partition-与-consumer-的关系" tabindex="-1"><a class="header-anchor" href="#记录-partition-与-consumer-的关系"><span>记录 Partition 与 Consumer 的关系</span></a></h4><p>Consumer Group 在 ZooKeeper 上的注册节点为  <code>/consumers/[group_id]</code>，而 Consumer Group 中的 Consumer 在 ZooKeeper 上的注册节点为  <code>/consumers/[group_id]</code>  下的子节点 owners，它们共享一个 Group ID。为了 Consumer 负载均衡，同一个 Group 订阅的 Topic 下的任一 Partition 都只能分配给一个 Consumer。Partition 与 Consumer 的对应关系也需要在 ZooKeeper 中记录，路径为：</p><div class="language-javascript line-numbers-mode" data-ext="js" data-title="js"><pre class="language-javascript"><code><span class="token operator">/</span>consumers<span class="token operator">/</span><span class="token punctuation">[</span>group_id<span class="token punctuation">]</span> <span class="token operator">/</span> owners <span class="token operator">/</span> <span class="token punctuation">[</span>topic<span class="token punctuation">]</span> <span class="token operator">/</span> <span class="token punctuation">[</span>broker_id <span class="token operator">-</span> partition_id<span class="token punctuation">]</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>补充：这个路径也是一个临时节点，进行 Rebalance 时会被删除，而后依据新的对应关系重建。此外，<code>[broker_id-partition_id]</code>  是一个消息分区的标识，其内容就是该消息分区消费者的 Consumer ID，通常采用  <code>hostname:UUID</code>  形式表示。</p><h2 id="全程解析-producer-kafka-consumer" tabindex="-1"><a class="header-anchor" href="#全程解析-producer-kafka-consumer"><span>全程解析（Producer-kafka-Consumer）</span></a></h2><h3 id="producer-发布消息" tabindex="-1"><a class="header-anchor" href="#producer-发布消息"><span>Producer 发布消息</span></a></h3><p>Producer 采用 Push 模式将消息发布到 Kafka Broker，根据负载均衡算法（如轮询、Hash 等），这些消息将均衡写入到相应 Topic 对应的各个 Partition 中。在存储层面，采用顺序写磁盘（即 Append）模式写入。详细流程如下：</p><ol><li>Producer Push 消息，基于负载均衡算法获得目标 Partition 后，Producer 先从 ZooKeeper 的  <code>/brokers/.../state</code>  节点找到该 Partition 的 Leader；</li><li>Producer 将消息发送给该 Leader；</li><li>Leader 将消息写入本地 Log；</li><li>所有 Follower 主动从 Leader Pull 消息，写入本地 Log 后向 Leader 发送 ACK；</li><li>Leader 收到所有 ISR 中所有 Replica 的 ACK 后，更新 HW（High Watermark，最后 Commit 的 Offset），并向 Producer 发送 ACK；</li><li>Producer 接到 ACK，确认发送成功。</li></ol><h3 id="broker-存储消息" tabindex="-1"><a class="header-anchor" href="#broker-存储消息"><span>Broker 存储消息</span></a></h3><p>Topic 是逻辑概念，而 Topic 对应的 Partition 则是物理概念，每个 Partition 在存储层面都对应一个文件夹（目录）。由于 Partition 并不是最终的存储粒度，该文件夹下还有多个 Segment（消息索引和数据文件，它们是真正的存储文件）。</p><h3 id="consumer-消费消息" tabindex="-1"><a class="header-anchor" href="#consumer-消费消息"><span>Consumer 消费消息</span></a></h3><p>前面介绍过，目前采用的高级 API，Consumer 在消费消息时，只需指定 Topic 即可，API 内部实现负载均衡，并将 Offset 记录到 ZooKeeper 上。</p><p>值得一提的是，Consumer 采用 Pull 模式从 Broker 中读取数据，这是一种异步消费模式，与 Producer 采用的 Push 模式全然不同。Push 模式追求速度，越快越好，当然它取决于 Broker 的性能，而 Pull 模式则是追求自适应能力，Consumer 根据自己的消费能力消费。</p>`,115),u=[l];function k(m,h){return a(),r("div",null,u)}const P=e(d,[["render",k],["__file","Kafka原理.html.vue"]]),L=JSON.parse('{"path":"/%E8%AE%A1%E7%AE%97%E6%9C%BA/Kafka/Kafka%E5%8E%9F%E7%90%86.html","title":"Kafka 原理","lang":"zh-CN","frontmatter":{"date created":"2022-11-24 18:01","date updated":"2022-11-24 18:11"},"headers":[{"level":2,"title":"概念","slug":"概念","link":"#概念","children":[{"level":3,"title":"Topic & Partition","slug":"topic-partition","link":"#topic-partition","children":[]}]},{"level":2,"title":"Kafka 高可靠性实现基础","slug":"kafka-高可靠性实现基础","link":"#kafka-高可靠性实现基础","children":[{"level":3,"title":"Kafka 文件存储机制","slug":"kafka-文件存储机制","link":"#kafka-文件存储机制","children":[]},{"level":3,"title":"复制原理和同步方式","slug":"复制原理和同步方式","link":"#复制原理和同步方式","children":[]},{"level":3,"title":"Kafka 消息生产的可靠性","slug":"kafka-消息生产的可靠性","link":"#kafka-消息生产的可靠性","children":[]},{"level":3,"title":"Leader 选举机制","slug":"leader-选举机制","link":"#leader-选举机制","children":[]},{"level":3,"title":"Kafka 架构中 ZooKeeper 以怎样的形式存在？","slug":"kafka-架构中-zookeeper-以怎样的形式存在","link":"#kafka-架构中-zookeeper-以怎样的形式存在","children":[]},{"level":3,"title":"负载均衡","slug":"负载均衡","link":"#负载均衡","children":[]}]},{"level":2,"title":"全程解析（Producer-kafka-Consumer）","slug":"全程解析-producer-kafka-consumer","link":"#全程解析-producer-kafka-consumer","children":[{"level":3,"title":"Producer 发布消息","slug":"producer-发布消息","link":"#producer-发布消息","children":[]},{"level":3,"title":"Broker 存储消息","slug":"broker-存储消息","link":"#broker-存储消息","children":[]},{"level":3,"title":"Consumer 消费消息","slug":"consumer-消费消息","link":"#consumer-消费消息","children":[]}]}],"git":{"createdTime":1713887535000,"updatedTime":1713887535000,"contributors":[{"name":"LYPvvvLYP","email":"1909266427@qq.com","commits":1}]},"readingTime":{"minutes":22,"words":6601},"filePathRelative":"计算机/Kafka/Kafka原理.md","localizedDate":"2024年4月23日","excerpt":"\\n<h2>概念</h2>\\n<p></p>\\n<p>在 Kafka 架构中，有几个术语需要了解下。</p>\\n<ul>\\n<li><strong>Producer：</strong>&nbsp; 生产者，即消息发送者，Push 消息到 Kafka 集群的 Broker（就是 Server）中；</li>\\n<li><strong>Broker：</strong>&nbsp;Kafka 集群由多个 Kafka 实例（Server）组成，每个实例构成一个 Broker，其实就是服务器；</li>\\n<li><strong>Topic：</strong>&nbsp;Producer 向 Kafka 集群 Push 的消息会被归于某一类别，即 Topic。本质上，这只是一个逻辑概念，面向的对象是 Producer 和 Consumer，Producer 只需关注将消息 Push 到哪一个 Topic 中，而 Consumer 只需关心自己订阅了哪个 Topic；</li>\\n<li><strong>Partition：</strong>&nbsp; 每个 Topic 又被分为多个 Partition，即物理分区。出于负载均衡的考虑，同一个 Topic 的 Partition 分别存储于 Kafka 集群的多个 Broker 上。而为了提高可靠性，这些 Partition 可以由 Kafka 机制中的 Replicas 来设置备份的数量。如上面框架图所示，每个 Partition 都存在两个备份；</li>\\n<li><strong>Consumer：</strong>&nbsp; 消费者，从 Kafka 集群的 Broker 中 Pull 消息、消费消息；</li>\\n<li><strong>Consumer Group：</strong>&nbsp;High-Level Consumer API 中，每个 Consumer 都属于一个 Consumer Group，每条消息只能被 Consumer Group 中的一个 Consumer 消费，但可以被多个 Consumer Group 消费；</li>\\n<li><strong>Replicas：</strong>&nbsp;Partition 的副本，保障 Partition 的高可用；</li>\\n<li><strong>Leader：</strong>&nbsp;Replicas 中的一个角色， Producer 和 Consumer 只与 Leader 交互；</li>\\n<li><strong>Follower：</strong>&nbsp;Replicas 中的一个角色，从 Leader 中复制数据，作为它的副本，同时一旦某 Leader 挂掉，便会从它的所有 Follower 中选举出一个新的 Leader 继续提供服务；</li>\\n<li><strong>Controller：</strong>&nbsp;Kafka 集群中的一个服务器，用来进行 Leader Election 以及各种 Fail Over；</li>\\n<li><strong>Zookeeper：</strong>&nbsp;Kafka 通过 ZooKeeper 存储集群的 Meta 信息等，文中将详述。</li>\\n</ul>"}');export{P as comp,L as data};
